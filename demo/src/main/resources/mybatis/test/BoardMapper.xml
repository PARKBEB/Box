<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.BoardMapper">
	<select id="list" resultType="BoardVo">
		select 
			no, id, name, rank, hit, date, time, @ROWNUM := @ROWNUM + 1 AS rownum 
		from board, (select @rownum:=0) temp
	</select>
	
	<insert id="insert">
		insert into board (id,name,rank,hit,date,time)
				values (#{id},#{name},#{rank},#{hit},NOW(),NOW())
	</insert>
	
	<select id="detail" resultType="BoardVo">
		select 
			no, id, name, rank, date, time
		from board where no = #{no}
	</select>
	
	<update id="update">
		update board
		set name = #{name}, rank = #{rank}, id = #{id}, time = NOW(), date = NOW()
		where no = #{no}
	</update>
	
	<delete id="delete">
		delete from board 
		where no = #{no}
	</delete>
	
	<update id="updateHit" >
		update board
		set hit = hit+1
		where no = #{no}
	</update>
	
	<select id="search">
		select * from board 
		where id like %{keyword}% or name like %{keyword}% or rank like %{keyword}%
	</select>
</mapper>



